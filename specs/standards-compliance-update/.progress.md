---
spec: standards-compliance-update
basePath: specs/standards-compliance-update
phase: requirements
task: 0/0
updated: 2026-03-06T12:00:00
---

# Progress: standards-compliance-update

## Original Goal

Standards compliance update v3.0.0 -- Implement items 1, 2, 3, 5, 6, 8 from specs/standards-compliance-update.md. Item 1: migrate getExtraMenuOptions to getNodeMenuItems across 5 JS files + rename _buildColorMenu. Item 2: standardize extension names to cx.sliders.<type>. Item 3: Option A -- restructure slider bank to single JSON input, eliminate hidden widgets (breaking change). Item 5: add SEARCH_ALIASES (V1) and search_aliases (V3) to all nodes. Item 6: create docs/ directory with per-node help markdown files. Item 8: add classifiers to pyproject.toml. Version bump to v3.0.0. Do NOT modify repository URL in pyproject.toml.

## Completed Tasks

- Research phase complete (research.md created) -- approved
- Requirements phase complete (requirements.md created) -- approved
- [x] 1.1 Rename extension name in cxslider.js
- [x] 1.4 Rename extension name in cxseed.js
- [x] 1.2 Rename extension name in cxdial.js
- [x] 1.3 Rename extension name in cxtoggle.js
- [x] 1.5 Rename extension name in cxsliderbank.js
- [x] 1.7 Add classifiers to pyproject.toml
- [x] 1.8 Add SEARCH_ALIASES to cxsliders.py (V1 + V3)
- [x] 1.9 Add SEARCH_ALIASES to cxdial.py (V1 + V3)
- [x] 1.11 Add SEARCH_ALIASES to cxseed.py (V1 + V3)
- [x] 1.10 Add SEARCH_ALIASES to cxtoggle.py (V1 + V3)
- [x] 1.12 Add SEARCH_ALIASES to cxsliderbank.py (V1 + V3)
- [x] 1.14 Rename _buildColorMenu to _buildColorMenuItems in cx_base_widget.js + callers
- [x] 1.16 Migrate cxslider.js: remove getExtraMenuOptions, add getNodeMenuItems

## Current Task

Awaiting next task

## Learnings

### Verification: 1.6 [VERIFY] Quality checkpoint: JS syntax after extension renames
- Status: PASS
- Commands: node --input-type=module --check on all 5 JS files (cxslider.js, cxdial.js, cxtoggle.js, cxseed.js, cxsliderbank.js) - all exit 0
- No fixes needed

### Verification: 1.13 [VERIFY] Quality checkpoint: Python syntax after search aliases
- Status: PASS
- Commands: python AST parse on cxsliders.py, cxdial.py, cxtoggle.py, cxseed.py, cxsliderbank.py - all pass
- No fixes needed

- Help page docs must go in WEB_DIRECTORY/docs/, NOT web/docs/. Since WEB_DIRECTORY="./js", the correct path is js/docs/. The spec document was wrong about this.
- No installed custom node packs (kjnodes, rgthree, impact-pack) have help page markdown files in the official format yet -- all their docs/ directories contain images or other non-help-page content
- V1 SEARCH_ALIASES is read by server.py via getattr(obj_class, 'SEARCH_ALIASES', []) -- just a class attribute, no registration needed
- V3 search_aliases is a parameter on io.Schema (confirmed in _io.py L1393), field(default_factory=list)
- getNodeMenuItems confirmed present in frontend v1.38.13 (found in core-CRhwbZ1e.js and dialogService files)
- cxToggle does NOT use _buildColorMenu from CxNumericWidget -- it builds color picker items inline because it extends CxBaseWidget directly, not CxNumericWidget
- slider_count is NOT a backend input in sliderbank -- it's JS-only (node.properties). Backend always returns all 8 outputs.
- Widget value as object is valid per widget-serialization-finding.md; arrays are not (they signal links). serializeValue() can return JSON.stringify() for string-type inputs.
- Version bump needs 4 locations: pyproject.toml, js/cx_utils.js CX_VERSION, CHANGELOG.md, README.md
- Item 3 (slider bank) must be implemented LAST because it's the only breaking change and Item 6 help page content depends on the post-restructure schema
- onConfigure migration for slider bank should handle arrays shorter than 8 elements (fill missing with 0) -- older workflows may have fewer sliders
- Separator (null) in getNodeMenuItems return array needs manual verification -- confirmed for getExtraMenuOptions but not yet for new API
- cxToggle color menu should stay inline in getNodeMenuItems for now; refactoring to share _buildColorMenuItems is out of scope for this release

## Blockers

- None currently

### Requirements Interview (from requirements.md)
- Migration strategy (Item 3): Best-effort onConfigure migration -- detect old 8-number format and convert to JSON, accept silent failures at edges
- Help page depth (Item 6): Comprehensive -- full docs with description, inputs/outputs, properties, interaction patterns, right-click menu items
- Delivery strategy: All 6 items together as v3.0.0 -- single release since Item 3 forces major bump anyway
- Chosen approach: Single v3.0.0 release with best-effort migration

### Design Interview (from design.md)
- Color menu API: Keep push-based -- rename `_buildColorMenu` to `_buildColorMenuItems`, minimal change
- Slider bank deserialization: Use `serializeValue()`/`deserializeValue()` pair -- cleaner separation, `deserializeValue` parses JSON string back to object
- JSON error handling: Raise `ValueError` -- let ComfyUI framework show error dialog, most visible to user
- Chosen approach: Incremental file-by-file changes following dependency order, no new abstractions

## Learnings (Design Phase)
- Slider bank widget name must change from `"cx_bank_ui"` to `"values"` to match the Python input name -- framework auto-serialization requires name match
- `serialize: false` in current CxSliderBankWidget constructor must become `serialize: true` (implicit when not set) for the new pattern to work
- `deserializeValue()` needed alongside `serializeValue()` -- framework passes raw string from `widgets_values`, widget must parse back to object
- v2.x migration detection: `typeof wv[0] === 'number'` is more robust than `wv.length === 8` since old workflows may have varying widget counts
- `getNodeMenuItems` is a top-level extension property (not on `nodeType.prototype`) -- receives `node` param, returns array, framework merges from all extensions
- `isInteger` in menu items resolved via existing module-level maps using `node.comfyClass` -- no closure dependency in new API

### Tasks Interview (from tasks.md)
- Testing thoroughness: Syntax-only -- JS node --check + Python AST parse after major items, manual testing deferred to end
- E2E verification: YES -- VE checkpoint tasks with syntax validation after each major phase
- Execution priority: Balanced -- clean code with reasonable validation at each step
- Task granularity: Fine -- 40-60 small atomic tasks, one commit each
- Chosen approach: Design order (Items 2, 8, 5, 1, 3, 6, version bump) -- user deferred to default

## Learnings (Task Planning)
- 51 tasks total across 4 phases: Phase 1 (51 impl tasks with checkpoints), Phase 3 (quality gates + VE), Phase 4 (PR lifecycle). Phase 2 skipped (no test framework).
- Item 1 menu migration for sliderbank (task 1.22) initially keeps old widget name `cx_bank_ui`; task 1.36 updates to `"values"` after Item 3 restructure -- ordering matters
- Item 3 slider bank tasks split into 4 Python tasks (V1 Int, V1 Float, V3 Int, V3 Float) + 8 JS tasks for granularity
- Parallel groups: extension renames (1.1-1.5), search aliases (1.8-1.11), help pages (1.39-1.43, 1.45-1.46)
- All verify commands use `node --input-type=module --check` for JS and `python -c "import ast; ast.parse(...)"` for Python -- no other quality tools available
- grep-based VE tasks substitute for real E2E since no dev server or automated E2E tooling exists

## Next

Tasks approved -- ready for `/ralph-specum:implement`
